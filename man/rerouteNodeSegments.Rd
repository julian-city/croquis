% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-route-nodes.R
\name{rerouteNodeSegments}
\alias{rerouteNodeSegments}
\title{Reroute segments around a moved node}
\usage{
rerouteNodeSegments(
  nodes,
  points,
  node_idx,
  new_lng,
  new_lat,
  drawing_mode,
  routing_server
)
}
\arguments{
\item{nodes}{Data frame of route nodes with at minimum: \code{node_id}, \code{lng},
\code{lat}, \code{is_stop}, \code{stop_id}, \code{stop_name}, \code{speed_factor}, \code{index}.}

\item{points}{Data frame of route points with columns: \code{index}, \code{lng},
\code{lat}.}

\item{node_idx}{Integer. Row index (not \code{node_id}) of the node being moved.}

\item{new_lng}{Numeric. New longitude for the node.}

\item{new_lat}{Numeric. New latitude for the node.}

\item{drawing_mode}{Character. Either \code{"network"} or \code{"free"}.}

\item{routing_server}{Character. The routing server to use (e.g. \code{"Valhalla"}, \code{"OSRM"}).}
}
\value{
A list with elements \code{nodes} and \code{points}, each a data frame with
updated coordinates and indices. Row names are reset to
\code{1:nrow(...)}.
}
\description{
Recalculates the route point geometry when a node (stop or waypoint) is
moved to new coordinates. Handles all positional cases: single node, first
node, last node, and middle node.
}
\details{
All existing node attributes (node_id, is_stop, stop_id, etc.) are
preserved on the moved node. Callers that need to change attributes (e.g.,
converting a waypoint to a stop) should modify the returned \code{nodes} data
frame at row \code{node_idx} after the call.
}
\keyword{internal}
